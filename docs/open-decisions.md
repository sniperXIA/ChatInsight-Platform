# ChatInsight 未决决策与设计记录 (Open Decisions)

| 编号 | 事项 | 当前决策 | 备选方案 | 状态 |
|---|---|---|---|---|
| OD-001 | 本地开发数据库驱动 | 采用 SQLAlchemy 2.0 Async，支持 SQLite (WAL) 本地单机即开即用与 PostgreSQL+pgvector 多进程部署 | 仅支持 PostgreSQL | 已确定 |
| OD-002 | 历史数据中的 `[图片]` 映射策略 | 仅在时间窗口唯一且无冲突时自动关联并标记算法置信度；多候选或存在歧义时标记 `needs_review` 供人工确认 | 强行按时间顺序一对一绑定（已否决） | 已确定 |
| OD-003 | 用户昵称与身份隔离 | 数据库加密存储昵称 Hash 与原始加密字符串，业务层对外输出加盐 HMAC-SHA256 匿名标签（如“用户 U-6F31”） | 直接使用真实微信群昵称（已否决） | 已确定 |
